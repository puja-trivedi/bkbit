name: add dunder methods to genome_annotation model

on:
  push:
    # paths:
    #   - 'bkbit/models/genome_annotation.py'

permissions:
  contents: write

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9
    
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Add commands to install other dependencies if necessary
          # For example: pip install -r requirements.txt
      
      - name: Run add_dunderMethods_genomeAnnotation
        run: python model_editors/add_dunderMethods_genomeAnnotation.py
